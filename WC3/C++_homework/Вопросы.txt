1) При задании std::string text, на примере pen&paper, нужно ли делать ему ресайз, или он происходит автоматически при заполнении?
2) Хэдэры без инклудов в домашке C++ - так и надо, или поправлять?
3) Нужно ли в домашке реализовывать логику сверх той, что указана в подсказках? (к примеру, isReady = false при зарядке пистолета)
4) Домашку надо сдавать с main или без?

Date:

5) Enum или массив? Что лучше/быстрее - массив или enum со свитчкейсами?
6) Нужно ли все-таки записывать неправильную дату, или давать дефолтную?
7) string.size, string.length - в описании, что возвращает размер в байтах, а в Pen&Paper возвращает размер по количеству элементов. Оно всегда автоматически переводит из-за того, что я присваиваю это число инту?
8) Насколько уместно кидать try-catch прямо в методах класса? Примеры использования try-catch в реальной жизни, работа с областью видимости try-catch (чтобы не вызывать объект повторно если эксепшна не вышло)ю

9) Синтаксис объявления в классах (InvalidDate(const std::string& text) : text(text) {} и 
class OutOfRounds(const std::string& text) {
    public:
        //somecode
};

10) List.cpp - нужно ли использовать встроенную сортировку, или написать свою? Есть ли требования к этой сортировке (кроме а. сложности для частного случая программы)?

